# KB Setup Template

To initialize this KB structure in a new agent workspace:

1. Create directory structure:
   - `docs/daily/`
   - `docs/knowledge/concepts/`
   - `docs/knowledge/connections/`
   - `docs/knowledge/qa/`
2. Add `.gitkeep` in each empty directory.
3. Add `AGENTS.md` to root with schema definitions.
